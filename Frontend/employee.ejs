<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Employee Department Table</title>
</head>
<body>
  <h1>Employee  Table</h1>
  <table>
    <thead>
      <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Salary</th>
        <!-- Add more table headers as needed -->
      </tr>
    </thead>
    <tbody>
      <% data.forEach(employee => { %>
        <tr>
          <td><%= employee.id %></td>
          <td><%= employee.name %></td>
          <td><%= employee.salary %></td>
          <!-- Add more table cells as needed -->
        </tr>
      <% }); %>
    </tbody>
  </table>
</body>
</html>
